detect:
  waveform_files:
    - Example/XB.ELYSE.02.BHUVW_20190619T042225-20190701T000001.mseed

  detector:
    inventory_file: 'IRIS'             # Either path to file or data centre, e.g. 'IPGP', 'IRIS' ..
    taper_length_per_side: 0.1         # in percent
    pre_filt: False
    water_level: 60
    ACCfilter: 
      type : 'bandpass'
      options: {'freqmin':0.001,  'freqmax':0.1,  'corners':3, 'zerophase':True}
      string: 'bp: 0.001 < f < 0.1'
    window_length_minutes: 5
    average_peak_height_times: 4
    show_triggering: False            # If True, is not saved but only shown (one for each component of each 12h file part)
    glitch_min_dist: 5                # minimum distance in seconds between 2 glitches. Before that, no new glitch will be declared
    glitch_length: 25                 # only used to determine glitch end time 
    glitch_min_polarization: 0.8      # applied to calculated polarization of both gain corrected and displacement data (set=0 if not wished)
    plot_individual: False            # If True, is saved in current working directory but not shown


plot:
  glitch_detector_files:
    - Example/glitches_XB.ELYSE.02.BHUVW_20190619T042225-20190701T000001.txt

  plotter:
    glitch_overview_plot:
      run: True
      UNIT: GAI
      sols: [140,320]
      LMST_range: ['00:00:00', '24:00:00']
      min_amp: False
      max_amp: False
      show: True
      outfile: 'Example/overview.pdf'

    glitch_gutenberg_plot: 
      run: False
      UNIT: GAI
      sols: []
      LMST_range: ['00:00:00', '24:00:00']
      show: True
      outfile: ''

    glitch_waveform_plot:
      run: False
      sols: []
      LMST_range: ['00:00:00', '24:00:00']
      min_amp: False
      max_amp: False      
      scale: 5
      show: True
      outfile: False

    glitch_XoverBAZ_plot:
      run: False
      comp: Z
      UNIT: GAI
      sols: []
      LMST_range: ['0', '24']
      min_amp: False
      max_amp: False
      show: True
      outfile: ''

    glitch_align_plot:
      run: False
      sols: [140,320]
      LMST_range: ['21000', '020000']
      min_amp: False
      max_amp: False      
      mode: largest
      scale: 60
      align: maximumW
      show: True
      outfile: ''

    glitch_ppol_plot:
      run: False
      UNIT: GAI      
      glitch_number: 14315
      show: True
      outfile: ''


remove:
  # not yet implemented


evaluate:
  # not yet implemented
